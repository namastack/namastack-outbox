spring:
  application:
    name: namastack-outbox-example-flyway-jpa

  datasource:
    url: jdbc:h2:mem:demo;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driverClassName: org.h2.Driver
    username: sa
    password:

namastack:
  outbox:
    instance:
      graceful-shutdown-timeout-seconds: 2

logging:
  level:
    root: warn
