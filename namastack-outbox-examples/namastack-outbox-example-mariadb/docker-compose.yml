services:
  database:
    image: mariadb:12
    environment:
      MARIADB_DATABASE: outbox_example
      MARIADB_ROOT_PASSWORD: password
      MARIADB_USER: username
      MARIADB_PASSWORD: password
    ports:
      - "33306:3306"
